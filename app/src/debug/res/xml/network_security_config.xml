<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override.

  Still refuses cleartext by default; the single exception is a TMS mock or staging server running
  on the developer's own machine. This resource is not present in a release build, so a release APK
  physically cannot send cleartext even if someone mis-sets TMS_BASE_URL — and the build script
  rejects a non-https release URL before that anyway.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- The host machine, as seen from the Android emulator. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
    </domain-config>
</network-security-config>

interface:
  display_name: "1stclass App"
  short_description: "Repo-specific patterns and workflows for 1stclass-app"
  default_prompt: "Use the 1stclass-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true